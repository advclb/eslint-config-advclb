# Adventure Club Core ESLint Shareable Configuration

For Node.js, ES6, browser and Jest environments.

```
npm i -D eslint @advclb/eslint-config
```

Edit `.eslintrc.json`:

```json
{
  "extends": ["@advclb"]
}
```
